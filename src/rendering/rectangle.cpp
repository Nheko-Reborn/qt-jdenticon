#include "rectangle.h"

namespace rendering {

Rectangle::Rectangle(int x, int y, int width, int height)
{
    x_ = x;
    y_ = y;
    width_ = width;
    height_ = height;
}

} // namespace rendering
